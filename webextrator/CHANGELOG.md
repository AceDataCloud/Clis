# Changelog

## [0.1.0] - 2025-05-04

### Added
- Initial release
- `extract` command for extracting structured content from web pages
- `render` command for rendering web pages and returning HTML
- `tasks` command group for querying async task results (retrieve, batch)
- `config` command to display current configuration

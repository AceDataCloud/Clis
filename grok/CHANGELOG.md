# Changelog

## [0.1.0] - 2024-01-01

### Added
- Initial release
- `chat` command for Grok chat completions
- `video` command for video generation
- `task` and `tasks` commands for task status
- `wait` command for polling task completion
- `models` and `config` info commands

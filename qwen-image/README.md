# Qwen Image CLI

```bash
pip install qwen-image-cli
qwen-image generate "A cinematic poster" --size 1024*1024
qwen-image edit "Restyle" -i https://example.com/reference.png
qwen-image task TASK_ID
```

Supports `qwen-image-3.0` and `qwen-image-3.0-pro`.

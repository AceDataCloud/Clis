apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: qwen-image-cli
  name: qwen-image-cli
  namespace: acedatacloud
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: qwen-image-cli
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: qwen-image-cli
    spec:
      containers:
        - name: qwen-image-cli
          image: ghcr.io/acedatacloud/qwen-image-cli:${TAG}
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
      dnsPolicy: ClusterFirst
      restartPolicy: Always
